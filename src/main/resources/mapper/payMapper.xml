<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="forum.board.repository.mybatisMapper.PayMapper">

<insert id="savePayBill">
    insert into payments_point values (#{payForm.merchantUid},#{payForm.pg},#{payForm.amount},#{memberId})
</insert>


</mapper>